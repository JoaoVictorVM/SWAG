import { prata } from "@/components/layout/fonts"

export default function HeroSection() {
    return (
        <section className="{heroSection}">
            <h1 className={prata.className}>SWAG</h1>
        </section>
    )
}